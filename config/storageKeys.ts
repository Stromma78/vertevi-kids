// Centralised storage keys for AsyncStorage.
// Update these constants here if keys ever need to change.

export const SCREEN_TIME_RULES_KEY = 'vertevi:screenTimeRules';
export const POSTURE_SETTINGS_KEY = 'vertevi:postureSettings';

// Stores session summaries (e.g. distance, posture, movement sessions).
export const SESSIONS_KEY = 'vertevi:sessions';
